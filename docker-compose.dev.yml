services:

  server:
    build:
      dockerfile: dockerfile.dev
    ports:
      - "8080:8080"
    volumes:
      - ./:/app/
    depends_on:
      - postgres
    extra_hosts:
      - "host.docker.internal:host-gateway"
